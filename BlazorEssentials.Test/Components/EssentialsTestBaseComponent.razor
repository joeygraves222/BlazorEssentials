@inherits EssentialsBaseComponent

@code {
    [Inject]
    protected StateManager AppState { get; set; }

    protected override void OnInitialized()
    {
        base.OnInitialized();
        AppState.OnChange += StateHasChanged;
    }
}
